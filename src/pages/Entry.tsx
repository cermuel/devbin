import React from "react";

const Entry = () => {
  return (
    <div className="bg-pry w-screen h-screen">
      This is the entry page
      <button>loiujhygfd</button>
    </div>
  );
};

export default Entry;
